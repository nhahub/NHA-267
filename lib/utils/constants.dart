import 'package:flutter/material.dart';

// --- COLOR CONSTANTS ---
const Color kPrimaryColor = Color(0xFF1E88E5);

// --- DURATION CONSTANTS ---
// المدة الكلية لبقاء الشاشة الافتتاحية
const Duration kSplashDuration = Duration(seconds: 2);
// مدة التوسع الدائري
const Duration kAnimationDuration = Duration(milliseconds: 1000);
// تأخير بدء ظهور الـ curve وزر "ابدأ"
const Duration kCurveAnimationDelay = Duration(milliseconds: 200);